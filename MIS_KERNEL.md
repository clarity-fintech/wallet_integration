# CLRTY Wallet Integration

CLRTY-1 · chain **1202** · RPC https://rpc.clarity-fintech.com

Partner kits and SDKs — compile Moniversive modules with **`bin/misc` only** (see `MIS_KERNEL.md`).

```bash
make verify    # full node + SDK gate when deps installed
make nodes-verify
```

Downloads: see `DOWNLOADS.md`.
